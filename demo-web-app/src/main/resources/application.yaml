server:
  port: 8082

sdk:
  config:
    base-uri: "https://api-test.ksef.mf.gov.pl"
    suffix-uri: "v2"
    qr-uri: "https://qr-test.ksef.mf.gov.pl"
    request-timeout: 100
    lighthouse-base-uri: "https://api-latarnia-test.ksef.mf.gov.pl/"
    default-headers:
      # Wymuszenie formatu Problem Details dla odpowiedzi błędów 400 i 429
      X-Error-Format: "problem-details"

spring:
  profiles:
    default: local
